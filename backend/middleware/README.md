# middleware
Cross-cutting request handling: JWT verification + session validation
(§2 Rule 9), tenant/barangay scoping, role checks (§7), idempotency-key
handling, request logging (allow-listed fields only — §2 Rule 17, never
raw_narrative or credentials).

# prompts
Llama-SEA-LION-v3.5-8B-R prompt templates for the AI pipeline: redaction,
summarization, translation (§2 Rule 16 — raw → redaction draft → summary
→ Secretary review → approval; translation is a separate post-approval
job). Prompts are versioned alongside `model_version` values recorded in
`ai_processing_log`.

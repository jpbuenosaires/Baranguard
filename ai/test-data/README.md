# test-data
Synthetic PII only — never real incident data (§4 folder rule). Used for
local AI pipeline testing/evaluation (`ai_evaluation_run` rows) without
ever touching real `raw_narrative` content.
